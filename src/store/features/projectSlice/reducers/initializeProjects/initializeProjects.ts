import { PayloadAction } from '@reduxjs/toolkit'
import { projectStateType } from '../../projectSlice.types'

const initializeProjects = (state: projectStateType, action: PayloadAction) => {
    // do nothing
}

export default initializeProjects