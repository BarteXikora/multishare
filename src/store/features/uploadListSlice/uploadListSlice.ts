import { createSlice } from '@reduxjs/toolkit'

const uploadListSlice = createSlice({
    name: 'uploadSlice',
    initialState: [],
    reducers: {

    }
})

export default uploadListSlice.reducer
export const { } = uploadListSlice.actions