/**
 * WorkInProgress window
 * 
 * This window is shown when user attempts to do something, but the functionality it not
 * ready yet.
 */

const WorkInProgressWindow = () => {

    // Rendering the component:
    return <section>
        <h2>Prace trwają...</h2>

        <p>Ta funkcjonalność nie jest jeszcze gotowa.</p>
    </section>
}

export default WorkInProgressWindow